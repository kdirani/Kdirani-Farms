-- تنظيف اختياري - احذف هذا الملف إذا كنت تريد إزالة الدوال
-- ⚠️ تحذير: تأكد من أنك لن تحتاج هذه الدوال مستقبلاً

-- حذف الدوال القديمة (اختياري)
-- DROP FUNCTION IF EXISTS public.get_upcoming_alerts(uuid, integer);
-- DROP FUNCTION IF EXISTS public.get_active_alerts_for_farm(uuid, integer);

-- ملاحظة: هذا الملف للتنظيف الاختياري فقط
-- النظام يعمل بشكل مثالي بدون حذف هذه الدوال
